
class N1stack:
	def __init__(self,stacksize):
		self.stacknum = 1
		self.array = [0] * capacity
		self.size = [0] * size
		self.stacksize = stacksize
	
	def isEmpty(stacknum):
		return self.size == 0

	def isFull(stacknum):
		return self.size[stacknum] == self.stacksize

	def push(self, stacknum, value):
		if self.isEmpty():
	
	def indexOfTop(self, stacknum):
		offset = 
		return ()

class N1stack:
	def __init(self, stacksize):
		self.stacknum = 1
		self.array = [0] * stacksize
		self.size = [0] * stacknum
		self.capacity = stacksize

	def isEmpty(self, stacknum):
		return self.size[stacknum] == 0

	def isFull(self, stacknum):
		return self.size[stacknum] == self.capacity

	def indexOfTop(self, stacknum):
		if isEmpty(stacknum)
			return "Stackl is empty"
		offset = stacknum * self.capacity
		return (offset + self.size[stacknum])



